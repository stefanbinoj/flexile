<% company = @resource.invited_by.company_administrators.first&.company %>
<%= render("shared/contractor_invitation", company:, user: @resource, url: @resource.create_clerk_invitation,
                                           contractor: @resource.company_workers.find_by(company_id: company.id)) %>
